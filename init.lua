-- Bootstrap Lazy (plugin manager)
-- This will automatically load install/update/run all plugins definied in `./plugins`
require("config.lazy")

require("options")
require("keymaps")
require("commands")

